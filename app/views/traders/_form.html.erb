<%= form_for(@trader) do |f| %>
  <% if @trader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trader.errors.count, "error") %> prohibited this trader from being saved:</h2>

      <ul>
      <% @trader.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Save Trader', class: "btn btn-success", role: "button" %>
  </div>
<% end %>
